// Re-export the auto-generated Lovable Cloud Supabase client
// so existing imports `from "@/lib/supabase"` keep working.
export { supabase } from "@/integrations/supabase/client";
export const isSupabaseConfigured = true;
